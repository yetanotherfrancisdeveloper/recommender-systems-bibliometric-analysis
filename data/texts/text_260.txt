Disclaimer. BLS. Networking. Audits. BLS. Networking. Audits. BLS. Networking. Open Issues. Audits. • e(P + Q, R) = e(P, R)e(Q, R) • e(P, Q + R) = e(P, Q)e(P, R) 1. R = signature_to_point(signature) 2. If R is INVALID, return INVALID 3. If signature_subgroup_check(R) is INVALID, return INVALID 4. If KeyValidate(PK) is INVALID, return INVALID 5. xP = pubkey_to_point(PK) 6. Q = hash_to_point(message) 7. C1 = pairing(Q, xP) 8. C2 = pairing(R, P) 9. If C1 == C2, return VALID, else return INVALID PopVerify() = r × P − PKr × H2C(M) = (SK+ SK) × H2C(M) ConsenSys/teku/issues/4025 1/p is slashable validator() is slashable attestation data slash validator() process slashings() process attester slashing() process proposer slashing() 5.2.1 Static Keys Signatures Replay 5.2.2 No DoS Countermeasures DH(dest key, source pubkey) DH(dest key, source ephemeral pubkey) DH(source ephemeral key, dest pubkey) Version. Known Vulnerabilities. Degree. Language. Criticality. Popularity. SSDLC Quality. Lighthouse (Rust). Teku (Java). //scans.gradle.com/ https://mvnrepository.com/ Nimbus (Nim). Teku (Java). Nimbus (Nim)). Lighthouse. Nimbus. Prysm. Teku.